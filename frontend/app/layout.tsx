import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AutoML Studio - Next-Gen Automated Machine Learning",
  description: "A comprehensive Automated Machine Learning platform featuring dataset analysis, automated preprocessing, scalable training, and intuitive evaluation.",
  keywords: "AutoML, Machine Learning, Data Science, AI, Predictive Modeling, AI Studio",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-[#09090b] text-white">{children}</body>
    </html>
  );
}
